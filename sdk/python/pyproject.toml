[project]
name = "cogs-client"
version = "0.1.0"
description = "Worker SDK for Cogs — background jobs and cron for self-hosted apps"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
